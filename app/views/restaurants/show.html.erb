

<p id="notice"><%= notice %></p>

<div id="restuarants">
  <%= render @restaurant%>
</div>

 <% if current_user %>
  <h4>New reservation</h4>
  <%= render 'reservations/form' %>
<% else %>
  <h4>Sign Up to make a reservation<h4>
  <%= link_to 'Sign Up', new_user_path %>
 <% end %>